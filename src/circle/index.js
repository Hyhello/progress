/**
 * 作者：yeshengqiang
 * 时间：2019-06-23
 * 描述：环形进度条
 */

import render from './render';

const Circle = options => {
	this.$conf = options;
	// 开始绘制
	this.render();
};

render(Circle);

export default Circle;
